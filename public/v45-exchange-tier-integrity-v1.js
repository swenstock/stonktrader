(()=>{
'use strict';
if(window.__sbcExchangeTierIntegrityV1)return;window.__sbcExchangeTierIntegrityV1=true;
const CANON={junior:{label:'Jr Broker',legacy:'JR. STONKBROKER'},trader:{label:'Trader',legacy:'TRADER'},clerk:{label:'Clerk',legacy:'CLERK'},runner:{label:'Runner',legacy:'RUNNER'}};
function typeFromText(text){const u=String(text||'').toUpperCase();if(/JR\.?\s*(STONK\s*)?BROKER|JUNIOR/.test(u))return'junior';if(u.includes('TRADER'))return'trader';if(u.includes('CLERK'))return'clerk';if(u.includes('RUNNER'))return'runner';return null;}
function selectorType(){const selector=document.getElementById('ticketTypeSelector');if(!selector)return null;const active=selector.querySelector('button.active');if(!active||active.id==='sbcBadgeMarketTab')return null;return typeFromText(active.textContent);}
function ensureJuniorMarker(){const title=document.getElementById('marketTicketTitle');if(!title)return;const type=selectorType(),existing=title.querySelector('[data-sbc-junior-marker]');if(type!=='junior'){existing?.remove();return;}if(existing)return;const marker=document.createElement('span');marker.setAttribute('data-sbc-junior-marker','1');marker.textContent=' JUNIOR';marker.style.cssText='display:none!important';title.appendChild(marker);}
function normalizeVisibleJunior(root=document.getElementById('view-exchange')){if(!root)return;const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);for(const n of nodes){const p=n.parentElement;if(!p||p.closest('[data-sbc-junior-marker]'))continue;const old=n.nodeValue||'';const next=old.replace(/JR\.?\s*STONKBROKER/gi,'JR BROKER').replace(/JR\.?\s*STONK\s+BROKER(?!\s+BADGE)/gi,'JR BROKER');if(next!==old)n.nodeValue=next;}}
function syncLegacyGlobal(type=selectorType()){if(!type)return type;try{if(typeof activeTicketMarket!=='undefined')activeTicketMarket=CANON[type].legacy;}catch(_){}ensureJuniorMarker();return type;}
function canonicalRender(){const type=syncLegacyGlobal();const api=window.__SBC_TICKET_EXCHANGE_STAGE1_TEST;if(!type||!api?.fetchRealBook||!api?.renderBookFromBackend)return null;return api.fetchRealBook(type).then(book=>{if(book?.ticketType!==type)throw new Error(`Ticket book mismatch: requested ${type}, received ${book?.ticketType||'unknown'}`);syncLegacyGlobal(type);const out=api.renderBookFromBackend(book);ensureJuniorMarker();normalizeVisibleJunior();return out;});}
function wrapRender(){const original=window.renderTicketMarket;if(typeof original!=='function'||original.__sbcTierIntegrityV2)return;const wrapped=function(){const type=selectorType();if(type&&window.__SBC_TICKET_EXCHANGE_STAGE1_TEST?.fetchRealBook)return canonicalRender().catch(err=>{console.error('Canonical ticket render failed',err);return original.apply(this,arguments)});syncLegacyGlobal(type);return original.apply(this,arguments);};wrapped.__sbcTierIntegrityV2=true;window.renderTicketMarket=wrapped;}
function auditDom(){const type=selectorType();if(!type)return true;ensureJuniorMarker();normalizeVisibleJunior();const title=(document.getElementById('marketTicketTitle')?.textContent||'').toUpperCase();const expected=type==='junior'?'JUNIOR':CANON[type].legacy;return type==='junior'?title.includes('JUNIOR'):title.includes(expected);}
let auditQueued=false;
function scheduleAudit(){if(auditQueued)return;auditQueued=true;requestAnimationFrame(()=>{auditQueued=false;ensureJuniorMarker();normalizeVisibleJunior();});}
function run(){wrapRender();syncLegacyGlobal();auditDom();}
document.addEventListener('click',e=>{const b=e.target?.closest?.('#ticketTypeSelector button');if(!b||b.id==='sbcBadgeMarketTab')return;const type=typeFromText(b.textContent);if(type){try{if(typeof activeTicketMarket!=='undefined')activeTicketMarket=CANON[type].legacy;}catch(_){};}queueMicrotask(()=>{syncLegacyGlobal(type);wrapRender();window.renderTicketMarket?.();});},true);
window.addEventListener('sbc:exchange-rendered',scheduleAudit);
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();setTimeout(run,250);setTimeout(run,900);
window.__SBC_EXCHANGE_TIER_INTEGRITY_TEST={typeFromText,selectorType,syncLegacyGlobal,canonicalRender,ensureJuniorMarker,normalizeVisibleJunior,auditDom,scheduleAudit,CANON};
})();
