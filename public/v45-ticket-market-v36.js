(()=>{
'use strict';
if(window.__sbcTicketMarketV36)return;
window.__sbcTicketMarketV36=true;
window.__sbcTicketMarketV35=true;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const TYPE_LABELS={junior:'JR BROKER',trader:'TRADER',clerk:'CLERK',runner:'RUNNER'};
const NAME_TO_TYPE={'JR. STONKBROKER':'junior','JUNIOR':'junior','TRADER':'trader','CLERK':'clerk','RUNNER':'runner'};
let panel=null,timer=null,capturedAuth='';
const realBookCache=new Map();
function typeFromText(t){const u=String(t||'').toUpperCase();for(const [name,type] of Object.entries(NAME_TO_TYPE))if(u.includes(name))return type;return'runner'}
function currentType(){return typeFromText($('#marketTicketTitle')?.textContent||$('#ticketTypeSelector .active')?.textContent||'RUNNER')}
function view(){return $('#view-exchange')}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
function auth(){try{const token=localStorage.getItem('token');if(typeof token==='string'&&token.trim())return`Bearer ${token.replace(/^Bearer\s+/i,'').trim()}`}catch(_){}return capturedAuth||''}
function headerValue(headers,name){try{if(headers instanceof Headers)return headers.get(name)||'';if(Array.isArray(headers)){const x=headers.find(([k])=>String(k).toLowerCase()===name.toLowerCase());return x?.[1]||''}if(headers&&typeof headers==='object'){const k=Object.keys(headers).find(k=>k.toLowerCase()===name.toLowerCase());return k?headers[k]:''}}catch(_){}return''}
const nativeFetch=window.fetch.bind(window);
window.fetch=async function(input,init){const ah=headerValue(init?.headers,'authorization')||headerValue(input?.headers,'authorization');if(ah)capturedAuth=ah;return nativeFetch(input,init)};
async function api(path,opts={}){const headers={...(opts.headers||{})},a=auth();if(a)headers.Authorization=a;if(opts.body&&!headers['Content-Type'])headers['Content-Type']='application/json';const r=await nativeFetch(path,{...opts,headers}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Exchange request failed (${r.status})`);return d}
async function backendActive(type=currentType()){if(!auth())return[];try{const d=await api('/api/ticket-market/mine');const bids=(d.bids||[]).filter(x=>x.status==='active'&&x.ticketType===type).map(x=>({...x,source:'backend'}));const offers=(d.offers||[]).filter(x=>x.status==='active'&&x.ticketType===type).map(x=>({...x,source:'backend'}));return[...bids,...offers]}catch(_){return[]}}
async function activeOrders(type=currentType()){return backendActive(type)}
function priceOf(o){return Number(o.bidPrice??o.askPrice??o.price??0)}
function staticMarket(){try{return TICKET_MARKETS?.[activeTicketMarket]||null}catch(_){return null}}
function marketTypeFromName(name){return typeFromText(name)}
function mappedMarket(book,fallback){const fb=fallback||{bids:[],asks:[],last:0};return{bids:(book?.bids||[]).map(x=>Number(x.bidPrice??x.price)).filter(Number.isFinite),asks:(book?.offers||[]).map(x=>Number(x.askPrice??x.price)).filter(Number.isFinite),last:Number(fb.last||0)}}
async function fetchRealBook(type=currentType()){const book=await api(`/api/ticket-market/book/${type}`);realBookCache.set(type,book);return book}
function renderBookFromBackend(book){
  const type=book?.ticketType||marketTypeFromName(typeof activeTicketMarket!=='undefined'?activeTicketMarket:'RUNNER'),name=TYPE_LABELS[type]||type;
  const fallback=staticMarket()||{bids:[],asks:[],last:0};
  const bids=(Array.isArray(book?.bids)?book.bids:[]).filter(o=>!o.isMine),offers=(Array.isArray(book?.offers)?book.offers:[]).filter(o=>!o.isMine);
  try{if(typeof hydrateExchangeTierIcons==='function')hydrateExchangeTierIcons()}catch(_){}
  const title=document.getElementById('marketTicketTitle');
  if(title){const visual=typeof exchangeVisualHTML==='function'?exchangeVisualHTML(name):'';title.innerHTML=visual+`<span style="display:inline-flex;align-items:center;gap:10px;vertical-align:middle">${String(name).toUpperCase()} TICKET MARKET</span>`}
  const summaryBid=document.getElementById('summaryBid'),summaryAsk=document.getElementById('summaryAsk'),summaryLast=document.getElementById('summaryLast');
  if(summaryBid)summaryBid.textContent=book?.highestBid!=null?Number(book.highestBid).toLocaleString():'—';
  if(summaryAsk)summaryAsk.textContent=book?.lowestAsk!=null?Number(book.lowestAsk).toLocaleString():'—';
  if(summaryLast)summaryLast.textContent=Number(fallback.last||0).toLocaleString();
  const visual=()=>typeof exchangeVisualHTML==='function'?exchangeVisualHTML(name):'';
  const askBook=document.getElementById('askBook');
  if(askBook)askBook.innerHTML=offers.map(o=>`<div class="book-row"><div class="book-ticket-meta">${visual()}<div><strong>1 ${name} Ticket Offered</strong><small>Seller #${String(o.id).slice(-4)} • Offer listed for sale</small></div></div><div class="book-price ask">${priceOf(o).toLocaleString()} STONK</div><button class="hit-ask" onclick="buyAtAsk(${priceOf(o)})">BUY OFFER</button></div>`).join('');
  const bidBook=document.getElementById('bidBook');
  if(bidBook)bidBook.innerHTML=bids.map(o=>`<div class="book-row"><div class="book-ticket-meta">${visual()}<div><strong>1 ${name} Ticket Bid</strong><small>Bidder #${String(o.id).slice(-4)} • Buyer posted this bid</small></div></div><div class="book-price bid">${priceOf(o).toLocaleString()} STONK</div><button class="hit-bid" data-sbc-bid-id="${o.id}" data-sbc-bid-price="${priceOf(o)}" onclick="sellIntoBid(${priceOf(o)},${o.id})">SELL TO BID</button></div>`).join('');
  const recent=document.getElementById('recentTicketSales'),fbBids=Array.isArray(fallback.bids)?fallback.bids:[],fbAsks=Array.isArray(fallback.asks)?fallback.asks:[];
  if(recent&&Number(fallback.last)>0){const sales=[fallback.last,Math.round((fallback.last+(fbBids[0]||fallback.last))/2),Math.round((fallback.last+(fbAsks[0]||fallback.last))/2)];recent.innerHTML=sales.map((p,i)=>`<div class="market-row"><div class="ticket-name">${typeof exchangeVisualHTML==='function'?exchangeVisualHTML(name,'ticket-badge'):''}<div><b>${name}</b><small style="display:block;color:#9dacb8">1 ticket transferred</small></div></div><div>${p.toLocaleString()} STONK</div><div>${[3,17,42][i]}m ago</div><div></div><div></div></div>`).join('')}
  const out=mappedMarket(book,fallback);
  window.dispatchEvent?.(new CustomEvent('sbc:exchange-rendered',{detail:{source:'ticket-market-v36',ticketType:book?.ticketType||currentType()}}));
  return out;
}
const originalTicketMarket=typeof window.ticketMarket==='function'?window.ticketMarket:null;
const originalRenderTicketMarket=typeof window.renderTicketMarket==='function'?window.renderTicketMarket:null;
if(originalTicketMarket){window.ticketMarket=function(){const type=marketTypeFromName(typeof activeTicketMarket!=='undefined'?activeTicketMarket:'RUNNER'),book=realBookCache.get(type);return book?mappedMarket(book,originalTicketMarket()):originalTicketMarket()}}
if(originalRenderTicketMarket){window.renderTicketMarket=function(){const type=marketTypeFromName(typeof activeTicketMarket!=='undefined'?activeTicketMarket:'RUNNER');return fetchRealBook(type).then(book=>renderBookFromBackend(book)).catch(()=>originalRenderTicketMarket())}}
const originalOpenMarketSell=typeof window.openMarketSell==='function'?window.openMarketSell:null;
if(originalOpenMarketSell){window.openMarketSell=function(){try{if(typeof ownedTicketContext!=='undefined'&&ownedTicketContext)ownedTicketContext={name:activeTicketMarket,ticketId:ownedTicketContext.ticketId}}catch(_){}return originalOpenMarketSell.apply(this,arguments)}}
function currentOfferTicketId(){try{if(typeof ticketOrder!=='undefined'&&ticketOrder?.listingId!==undefined&&ticketOrder?.listingId!==null&&ticketOrder?.listingId!=='')return ticketOrder.listingId}catch(_){}try{return ownedTicketContext?.ticketId}catch(_){return undefined}}
async function submitOffer(ticketId,askPrice){const price=Math.round(Number(askPrice));if(!(price>0))throw new Error('Enter a positive STONK price.');return api('/api/ticket-market/offers',{method:'POST',body:JSON.stringify({ticketId,askPrice:price})})}
async function submitCurrentOffer(askPrice){return submitOffer(currentOfferTicketId(),askPrice)}
async function submitBid(type,bidPrice){const price=Math.round(Number(bidPrice));if(!(price>0))throw new Error('Enter a positive STONK price.');return api('/api/ticket-market/bids',{method:'POST',body:JSON.stringify({ticketType:type,bidPrice:price})})}
let pendingBidAccept=null;
function realOwnedTicketId(){try{const raw=ownedTicketContext?.ticketId,n=Number(raw);return Number.isInteger(n)&&n>0?n:null}catch(_){return null}}
function cachedBidById(type,bidId){const book=realBookCache.get(type),id=Number(bidId);if(!book||!Number.isInteger(id))return null;return (book.bids||[]).find(b=>Number(b.id)===id&&!b.isMine)||null}
function cachedBestBid(type=currentType()){const book=realBookCache.get(type);if(!book)return null;return (book.bids||[]).find(b=>!b.isMine)||null}
async function acceptBid(bid,ticketId){if(!bid||bid.id===undefined||bid.id===null)throw new Error('That bid is no longer available. Refresh the ticket market and try again.');const id=Number(ticketId);if(!Number.isInteger(id)||id<=0)throw new Error('Choose the exact ticket you want to sell from My Tickets first.');return api(`/api/ticket-market/bids/${bid.id}/sell`,{method:'POST',body:JSON.stringify({ticketId:id})})}
function sellSummary(){return document.getElementById('sellChoiceSummary')}
function appendBidSuccess(result,bid){const el=sellSummary();if(!el)return;const price=Number(result?.soldFor??priceOf(bid));el.innerHTML += `<div style="margin-top:8px;padding:9px;border:1px solid #355a38;border-radius:7px;color:#73e76d;font-size:9px">✓ You accepted the buyer's ${price.toLocaleString()} STONK Bid. One ticket changes hands.</div>`}
async function settleBid(bid,ticketId){const result=await acceptBid(bid,ticketId);appendBidSuccess(result,bid);pendingBidAccept=null;schedule();await window.renderTicketMarket?.();return result}
function prepareSpecificBid(price,bidId){const type=currentType(),bid=cachedBidById(type,bidId),ticketId=realOwnedTicketId();if(!bid){alert('That real bid is not available in the current ticket book. Refresh and try again.');return null}if(!ticketId){alert('Choose the exact ticket you want to sell from My Tickets first.');return null}pendingBidAccept={bid,ticketId,type};try{ownedTicketContext={name:activeTicketMarket,ticketId}}catch(_){}
  const title=document.getElementById('sellChoiceTitle'),intro=document.getElementById('sellChoiceIntro'),summary=sellSummary(),button=document.getElementById('hitBidBtn'),modal=document.getElementById('sellChoiceModal'),p=priceOf(bid);
  if(title)title.textContent=`SELL ${String(activeTicketMarket||TYPE_LABELS[type]||type).toUpperCase()} TICKET`;
  if(intro)intro.textContent=`Accept this buyer's ${p.toLocaleString()} STONK Bid using one ${activeTicketMarket} ticket you own.`;
  if(summary)summary.innerHTML=`<div class="order-preview-row"><span>BUYER BID</span><b style="color:#69e660">${p.toLocaleString()} STONK</b></div><div class="order-preview-row"><span>TICKET REQUIRED</span><b>1 ${activeTicketMarket} Ticket</b></div><div class="order-preview-row"><span>RESULT</span><b>Your ticket → buyer • STONK → you</b></div>`;
  if(button)button.textContent=`ACCEPT ${p.toLocaleString()} STONK BID`;
  modal?.classList.add('open');return pendingBidAccept}
function realSellIntoBid(price,bidId){return prepareSpecificBid(price,bidId)}
async function realHitBestBid(){const type=currentType(),bid=pendingBidAccept?.bid||cachedBestBid(type),ticketId=pendingBidAccept?.ticketId||realOwnedTicketId();if(!bid)throw new Error('No real active bid is available. Refresh the ticket market and try again.');if(!ticketId)throw new Error('Choose the exact ticket you want to sell from My Tickets first.');return settleBid(bid,ticketId)}
window.sellIntoBid=realSellIntoBid;
window.hitBestBid=function(){return realHitBestBid().catch(err=>alert(err.message))};
function ensurePanel(){const v=view();if(!v)return null;if(panel&&!panel.isConnected)panel=null;if(panel)return panel;panel=$('#tm36Mine');if(panel)return panel;panel=document.createElement('section');panel.id='tm36Mine';panel.className='tm35-mine';const market=$$('section,article,div',v).find(el=>/BIDS\s*[—-]/i.test(el.textContent||'')&&/OFFERS\s*[—-]/i.test(el.textContent||''));if(market?.parentNode)market.parentNode.insertBefore(panel,market);else v.appendChild(panel);return panel}
function orderRow(o){const isBid=o.side==='bid',p=priceOf(o),source='backend';return`<div class="tm35-order"><div class="tm35-side ${isBid?'bid':'ask'}">${isBid?'BID':'LISTING'}</div><div class="tm35-order-main"><b>${esc(TYPE_LABELS[o.ticketType]||o.ticketType)} TICKET</b><span>${isBid?'You are bidding':'You are asking'} <strong>${p.toLocaleString()} STONK</strong></span></div><div class="tm35-actions"><button data-tm36-edit="${source}:${isBid?'bid':'offer'}:${o.id}:${p}">ADJUST</button><button class="danger" data-tm36-cancel="${source}:${isBid?'bid':'offer'}:${o.id}">CANCEL</button></div></div>`}
async function editOrder(spec){const [source,side,id,oldRaw]=spec.split(':'),old=Number(oldRaw),raw=prompt(`New ${side==='bid'?'bid':'ask'} price in STONK:`,String(old));if(raw===null)return;const price=Math.round(Number(String(raw).replace(/,/g,'')));if(!Number.isFinite(price)||price<=0)return alert('Enter a positive STONK price.');if(price===old)return;try{if(side==='bid')await api(`/api/ticket-market/bids/${id}`,{method:'PATCH',body:JSON.stringify({bidPrice:price})});else await api(`/api/ticket-market/offers/${id}`,{method:'PATCH',body:JSON.stringify({askPrice:price})});schedule();window.renderTicketMarket?.()}catch(e){alert(e.message)}}
async function cancelOrder(spec){const [source,side,id]=spec.split(':'),msg=`Cancel this ${side==='bid'?'bid':'listing'}?`;if(!confirm(msg))return;try{await api(side==='bid'?`/api/ticket-market/bids/${id}`:`/api/ticket-market/offers/${id}`,{method:'DELETE'});schedule();window.renderTicketMarket?.()}catch(e){alert(e.message)}}
async function renderBurn(){const p=ensurePanel();if(!p||!auth())return;let d;try{d=await api('/api/tickets')}catch(_){return}let box=$('.tm36-burn-box',p);if(!box){box=document.createElement('div');box.className='tm36-burn-box';p.appendChild(box)}const rows=['runner','clerk','trader'].map(type=>{const cfg=d.burnUpgrades?.[type],have=d.inventory?.[type]?.available||0;if(!cfg)return'';const enabled=have>=cfg.burnCount;return`<div class="tm35-order"><div class="tm35-order-main"><b>${cfg.burnCount} ${TYPE_LABELS[type]} TICKETS → ${TYPE_LABELS[cfg.targetType]} TICKET</b><span>${have} available • optional upgrade</span></div><div class="tm35-actions"><button data-tm36-burn="${type}" ${enabled?'':'disabled'}>${enabled?'BURN 10':'NEED '+(cfg.burnCount-have)}</button></div></div>`}).join('');box.innerHTML=`<div class="tm35-head"><div><small>OPTIONAL TICKET UPGRADE</small><h3>COLLECT 10 • MOVE UP</h3></div></div><div class="tm35-orders">${rows}</div>`;$$('[data-tm36-burn]',box).forEach(b=>b.onclick=()=>burnTickets(b.dataset.tm36Burn))}
async function burnTickets(type){if(!confirm(`Burn 10 ${TYPE_LABELS[type]} tickets for one higher-tier ticket? This cannot be undone.`))return;try{await api('/api/tickets/burn-upgrade',{method:'POST',body:JSON.stringify({sourceType:type,burnId:`web-${Date.now()}-${Math.random().toString(36).slice(2,8)}`})});await renderMine();}catch(e){alert(e.message)}}
async function renderMine(){const p=ensurePanel();if(!p)return;const type=currentType(),rows=await activeOrders(type);p.innerHTML=`<div class="tm35-head"><div><small>YOUR ACTIVE ORDERS • ${esc(TYPE_LABELS[type]||type)}</small><h3>MY ORDERS</h3></div><span>${rows.length} ACTIVE</span></div><div class="tm35-orders">${rows.length?rows.map(orderRow).join(''):'<div class="tm35-empty">No active bids or listings in this ticket market yet.</div>'}</div>`;$$('[data-tm36-edit]',p).forEach(b=>b.onclick=()=>editOrder(b.dataset.tm36Edit));$$('[data-tm36-cancel]',p).forEach(b=>b.onclick=()=>cancelOrder(b.dataset.tm36Cancel));await decorateMyTickets(rows);decorateBook(rows);renderBurn().catch(()=>{})}
async function decorateMyTickets(rows=[]){const v=view();if(!v)return;const h=$$('h1,h2,h3',v).find(x=>(x.textContent||'').trim().toUpperCase()==='MY TICKETS'),box=h?.parentElement;if(!box)return;box.classList.add('tm36-my-tickets');$$('.inv.big-inv',box).forEach(row=>row.remove());let d;try{d=await api('/api/tickets')}catch(_){return}const tickets=Array.isArray(d?.tickets)?d.tickets:[],types=['junior','trader','clerk','runner'];const back=$('.back-floor-cta',box);for(const type of types){const owned=tickets.filter(t=>t.ticket_type===type&&!['applied','consumed'].includes(t.status)),available=owned.filter(t=>t.status==='unredeemed'),listed=owned.filter(t=>t.status==='listed'),name=TYPE_LABELS[type]||type;const row=document.createElement('div');row.className='inv big-inv tm36-ticket-row';const visual=typeof exchangeVisualHTML==='function'?exchangeVisualHTML(name,'inv-art'):'';row.innerHTML=`<div class="inv-left">${visual}<div><b>${esc(name)} × ${owned.length}</b><span>${available.length} available${listed.length?` • ${listed.length} listed`:''}</span></div></div><div class="tm36-ticket-actions"></div>`;const actions=$('.tm36-ticket-actions',row);if(available.length){const ticket=available[0],sell=document.createElement('button');sell.className='tm36-native-sell';sell.textContent='SELL TO BID';sell.onclick=()=>openOwnedTicketSell(name,ticket.id);actions.appendChild(sell)}else{const status=document.createElement('button');status.disabled=true;status.textContent=listed.length?'LISTED':'NONE AVAILABLE';actions.appendChild(status)}box.insertBefore(row,back||null)}const selector=$('#ticketTypeSelector');if(selector){$$('button',selector).forEach(btn=>{if(/MAIN EVENT/i.test(btn.textContent||'')){btn.remove();return}const type=typeFromText(btn.textContent||''),count=d?.inventory?.[type]?.owned??0;const target=$('.ticket-filter-count',btn);if(target)target.innerHTML=`YOU OWN <b>${Number(count)}</b>`});try{if(typeof activeTicketMarket!=='undefined'&&/MAIN EVENT/i.test(String(activeTicketMarket))){activeTicketMarket='Runner';ownedTicketContext={name:'Runner',ticketId:null};const runner=$$('button',selector).find(b=>/RUNNER/i.test(b.textContent||''));$$('button',selector).forEach(b=>b.classList.remove('active'));runner?.classList.add('active');await window.renderTicketMarket?.()}}catch(_){}}}
function decorateBook(rows=[]){const type=currentType(),mine=rows.filter(o=>o.ticketType===type);for(const [side,selector] of [['bid','#bidBook'],['offer','#askBook']]){const book=$(selector);if(!book)continue;book.classList.add('tm36-book-scroll');$$('.tm36-my-book-row',book).forEach(x=>x.remove());mine.filter(o=>o.side===side).forEach(o=>{const div=document.createElement('div');div.className='book-row tm36-my-book-row';div.innerHTML=`<div class="book-ticket-meta"><div><strong>YOUR ${side==='bid'?'BID':'ASK'}</strong><small>Active order • manage in My Orders</small></div></div><div class="book-price ${side==='bid'?'bid':'ask'}">${priceOf(o).toLocaleString()} STONK</div><button data-tm36-book-edit="backend:${side}:${o.id}:${priceOf(o)}">ADJUST</button>`;book.prepend(div);$('[data-tm36-book-edit]',div).onclick=()=>editOrder($('[data-tm36-book-edit]',div).dataset.tm36BookEdit)})}}
function closeModal(root){if(!root)return;root.classList.remove('open');root.setAttribute('aria-hidden','true')}
function ensureModalControls(){['ticketOrderModal','bidOrderModal','sellChoiceModal'].forEach(id=>{const root=document.getElementById(id);if(!root)return;root.classList.add('tm35-market-modal');if(!$('.tm35-x',root)){const card=root.firstElementChild||root;card.style.position='relative';const x=document.createElement('button');x.type='button';x.className='tm35-x';x.setAttribute('aria-label','Close');x.textContent='×';x.onclick=e=>{e.preventDefault();e.stopPropagation();closeModal(root)};card.appendChild(x)}$$('button',root).forEach(b=>{if(/^CANCEL$/i.test((b.textContent||'').trim()))b.textContent='CLOSE'});if(!root.dataset.tm36Backdrop){root.dataset.tm36Backdrop='1';root.addEventListener('click',e=>{if(e.target===root)closeModal(root)})}})}
function captureNativeOrder(e){
  const b=e.target.closest('button');if(!b)return;
  if(b.classList.contains('hit-bid')&&b.dataset.sbcBidId){e.preventDefault();e.stopPropagation();if(typeof e.stopImmediatePropagation==='function')e.stopImmediatePropagation();prepareSpecificBid(b.dataset.sbcBidPrice,b.dataset.sbcBidId);return}
  const root=b.closest('#ticketOrderModal,#bidOrderModal,#sellChoiceModal');if(!root)return;
  const text=(b.textContent||'').trim().toUpperCase();
  if(!text||/^(CLOSE|CANCEL|×)$/.test(text)||b.classList.contains('tm35-x'))return;
  const type=currentType(); let action=null,closeAfter=true;
  if(root.id==='bidOrderModal'&&/(BID|PLACE|POST)/.test(text)){const price=$('#bidOrderPrice',root)?.value;action=()=>submitBid(type,price)}
  if(root.id==='ticketOrderModal'){let side='';try{side=String(ticketOrder?.side||'').toUpperCase()}catch(_){}if(side==='SELL'&&/(LIST|POST|ASK|CONFIRM)/.test(text)){const price=$('#ticketOrderPrice',root)?.value;action=()=>submitCurrentOffer(price)}}
  if(root.id==='sellChoiceModal'&&b.id==='hitBidBtn'){action=()=>realHitBestBid();closeAfter=false}
  if(!action)return;
  e.preventDefault();e.stopPropagation();if(typeof e.stopImmediatePropagation==='function')e.stopImmediatePropagation();
  Promise.resolve().then(action).then(()=>{if(closeAfter)closeModal(root);schedule();if(closeAfter)return window.renderTicketMarket?.()}).catch(err=>alert(err.message));
}
function schedule(){clearTimeout(timer);timer=setTimeout(()=>{ensureModalControls();renderMine().catch(()=>{})},90)}
document.addEventListener('click',captureNativeOrder,true);
document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;const root=['ticketOrderModal','bidOrderModal','sellChoiceModal'].map(id=>document.getElementById(id)).find(x=>x?.classList.contains('open'));if(root){e.preventDefault();closeModal(root)}});
window.__SBC_TICKET_EXCHANGE_STAGE1_TEST={fetchRealBook,renderBookFromBackend,submitOffer,submitCurrentOffer,submitBid,currentOfferTicketId,captureNativeOrder,getCachedBook:type=>realBookCache.get(type),getOwnedTicketId:()=>{try{return ownedTicketContext?.ticketId}catch(_){return undefined}}};
window.__SBC_TICKET_EXCHANGE_STAGE2_TEST={acceptBid,realHitBestBid,realSellIntoBid,prepareSpecificBid,cachedBidById,cachedBestBid,realOwnedTicketId,captureNativeOrder,getPendingBid:()=>pendingBidAccept};
function run(){ensureModalControls();schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();if(typeof window.addEventListener==='function')window.addEventListener('sbc:exchange-rendered',()=>schedule());document.addEventListener('click',e=>{if(e.target?.closest?.('#ticketTypeSelector button'))setTimeout(schedule,0)},true);setTimeout(run,400);setTimeout(run,1200);
})();
