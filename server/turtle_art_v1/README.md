# Turtle tier art v1

Canonical binary source assets for the five SBC tier portraits. `server/v45ExactShell.js` injects these PNG payloads into the existing `TIER_DATA[*].art` fields after exact-shell integrity verification; all live consumers continue to read only `TIER_DATA`.
