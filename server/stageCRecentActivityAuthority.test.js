const assert=require('assert');const fs=require('fs'),vm=require('vm');const src=fs.readFileSync('public/v45-advanced-orders-v15.js','utf8');const m=src.match(/function activityEvents\(\)\{[\s\S]*?\}\nfunction installBlotterStyle/);assert(m,'activityEvents function not found');const fn=m[0].replace(/\nfunction installBlotterStyle[\s\S]*$/,'');const sandbox={cache:{orders:[{side:'buy',symbol:'FAKEQ',status:'pending',orderType:'market',createdAt:'2026-08-27T10:00:00Z'},{side:'sell',symbol:'FAKEW',status:'pending',orderType:'limit',limitPrice:99,createdAt:'2026-08-27T10:01:00Z'},{side:'buy',symbol:'FAKEC',status:'cancelled',orderType:'stop',stopPrice:101,cancelledAt:'2026-08-27T10:02:00Z'},{side:'buy',symbol:'FAKEF',status:'executed',orderType:'limit',executedPrice:88,executedAt:'2026-08-27T10:03:00Z'}],trades:[{side:'buy',symbol:'BA',quantity:12.424242,price:188.25,timestamp:'2026-08-27T10:04:00Z'}]},money:n=>`$${Number(n||0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}`,Number};vm.createContext(sandbox);vm.runInContext(fn+';this.activityEvents=activityEvents;',sandbox);const rows=sandbox.activityEvents();assert.equal(rows.length,1,'Recent Activity must contain only completed trades');assert.deepStrictEqual(JSON.parse(JSON.stringify(rows[0])),{side:'buy',symbol:'BA',qty:'12.4242',price:'$188.25',status:'FILLED',at:'2026-08-27T10:04:00Z'});assert(!rows.some(r=>/^FAKE/.test(r.symbol)),'order lifecycle rows must not leak into Recent Activity');assert(src.includes("api(`/portfolios/${pid}/trades`)"),'Recent Activity authority endpoint must remain /portfolios/:id/trades');console.log('Stage C Recent Activity backend authority: PASS');