const fs=require('fs');
const js=fs.readFileSync('public/v45-desktop-stage51-v55.js','utf8');
const core=fs.readFileSync('public/v45-analytics-core-v1.js','utf8');
const css=fs.readFileSync('public/v45-desktop-stage51-v55.css','utf8');
const pre=fs.readFileSync('public/v45-desktop-stage46-v51-pre.js','utf8');
const must=(ok,msg)=>{if(!ok){console.error('FAIL:',msg);process.exit(1)}else console.log('PASS:',msg)};
must(core.includes("portfolio:['EQUITY CURVE','ALLOCATION BREAKDOWN','P&L DRIVERS']"),'portfolio analytics source requires the three real native chart signatures');
must(core.includes("advanced:['VS. PRIZE LINE','CASH DEPLOYMENT','RANK MOVEMENT']"),'advanced analytics source requires the three real native chart signatures');
must(core.includes('function findNativeModule(v,kind)')&&core.includes('hasNativeSignature'),'shared core selects native analytics modules by structural chart content');
must(core.includes('function isSafeNativeModule')&&core.includes('function ownsCorePortfolio'),'native analytics reuse requires signature match and core-workspace exclusion');
must(core.includes('function hasOtherNativeSignature')&&core.includes('!hasOtherNativeSignature(el,kind)'),'combined analytics ancestors are rejected');
must(core.includes('let outer=null,outerToggle=null')&&core.includes('return outerToggle||outer'),'analytics promotion selects the complete outer safe shell instead of an inner body');
must(core.includes("card.classList.remove('stage52-retired-analytics-stray-v56')"),'captured native panels are un-retired before modal use');
must(core.includes("card.removeAttribute('aria-hidden')"),'captured native panels are accessible in modal');
must(core.includes("if(marked&&isSafeNativeModule(marked,v,kind))return marked"),'previously marked sources are reused only when they remain safe real analytics modules');
must(js.includes('stage53-standard-expand-toggle-v57')&&js.includes("action.textContent=expanded?'COLLAPSE':'EXPAND'"),'analytics panels with native toggles share one labeled expand collapse control');
must(css.includes('.stage53-standard-expand-toggle-v57'),'shared analytics expand collapse styling is present');
must(!/wheel|pointermove|touchmove/.test(core+js),'stage53 does not add chart gesture handlers');
must(pre.includes('/v45-desktop-stage51-v55.js?v=63'),'corrected analytics owner is cache-busted');
console.log('Stage 53 native analytics validation complete.');
